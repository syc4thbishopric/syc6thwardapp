import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.sycamoresstake.6thward',
  appName: 'Sycamores 6th Ward App',
  webDir: 'out',
  server: {
    androidScheme: 'https'
  }
};

export default config;
